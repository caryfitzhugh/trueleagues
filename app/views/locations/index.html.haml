.page-header
  .pull-right
    %form{:method => :get, :action => new_location_path}
      %button.btn.btn-primary New Location
  %h1
    Locations
= render :partial => "index_list", :locals => {:locations => @locations }
